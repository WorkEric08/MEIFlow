{
  "framework": "vite",
  "buildCommand": "cd meiflow && npm install && chmod +x node_modules/.bin/* && npm run build",
  "outputDirectory": "meiflow/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }],
  "nodeVersion": "20.x"
}
